---
sections:
  - title: SpdxTool Requirements
    sections:
      - title: Platform Support
        requirements:
          - id: SpdxTool-Platform-Windows
            title: The tool shall run successfully on Windows platforms.
            tags:
              - platform
              - msbuild
            justification: |
              Windows is a widely used operating system for development. The tool
              (both the dotnet-tool and the MSBuild targets package) must function
              correctly on Windows to support a broad user base.
            tests:
              - windows@SpdxTool_Basic
              - windows@SpdxToolTargets_DecorateSbom_SingleTfm_True_DecoratesSbom
              - windows@SpdxToolTargets_DecorateSbom_MultiTfm_True_DecoratesSbom
              - windows@SpdxToolTargets_DecorateSbom_SingleTfm_False_SkipsDecoration
              - windows@SpdxToolTargets_GenerateSbom_SingleTfm_False_SkipsEntirely
              - windows@SpdxToolTargets_MissingWorkflow_SingleTfm_ReportsError

          - id: SpdxTool-Platform-Linux
            title: The tool shall run successfully on Linux platforms.
            tags:
              - platform
              - msbuild
            justification: |
              Linux is a widely used operating system for development and deployment,
              especially in CI/CD environments. The tool (both the dotnet-tool and
              the MSBuild targets package) must function correctly on Linux to support
              a broad user base.
            tests:
              - ubuntu@SpdxTool_Basic
              - ubuntu@SpdxToolTargets_DecorateSbom_SingleTfm_True_DecoratesSbom
              - ubuntu@SpdxToolTargets_DecorateSbom_MultiTfm_True_DecoratesSbom
              - ubuntu@SpdxToolTargets_DecorateSbom_SingleTfm_False_SkipsDecoration
              - ubuntu@SpdxToolTargets_GenerateSbom_SingleTfm_False_SkipsEntirely
              - ubuntu@SpdxToolTargets_MissingWorkflow_SingleTfm_ReportsError

          - id: SpdxTool-Platform-MacOS
            title: The tool shall run successfully on macOS platforms.
            tags:
              - platform
              - msbuild
            justification: |
              macOS is a widely used operating system for development, especially
              among software engineers and developers. The tool (both the dotnet-tool
              and the MSBuild targets package) must function correctly on macOS to
              support a broad user base.
            tests:
              - macos@SpdxTool_Basic
              - macos@SpdxToolTargets_DecorateSbom_SingleTfm_True_DecoratesSbom
              - macos@SpdxToolTargets_DecorateSbom_MultiTfm_True_DecoratesSbom
              - macos@SpdxToolTargets_DecorateSbom_SingleTfm_False_SkipsDecoration
              - macos@SpdxToolTargets_GenerateSbom_SingleTfm_False_SkipsEntirely
              - macos@SpdxToolTargets_MissingWorkflow_SingleTfm_ReportsError

          - id: SpdxTool-Platform-Net8
            title: The tool shall run successfully on .NET 8.x.
            tags:
              - platform
            justification: |
              .NET 8.x is a long-term support (LTS) release of .NET. The tool must
              support this version to provide stability and compatibility for users
              who require LTS support.
            tests:
              - dotnet8.x@SpdxTool_Basic

          - id: SpdxTool-Platform-Net9
            title: The tool shall run successfully on .NET 9.x.
            tags:
              - platform
            justification: |
              .NET 9.x is a current release of .NET. The tool must support this
              version to enable users to take advantage of the latest .NET features
              and improvements.
            tests:
              - dotnet9.x@SpdxTool_Basic

          - id: SpdxTool-Platform-Net10
            title: The tool shall run successfully on .NET 10.x.
            tags:
              - platform
            justification: |
              .NET 10.x represents forward compatibility. The tool should support
              future .NET releases to ensure users can upgrade their runtime without
              losing tool functionality.
            tests:
              - dotnet10.x@SpdxTool_Basic
