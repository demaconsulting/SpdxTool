---
sections:
  - title: SpdxTool Requirements
    sections:
      - title: SelfTest Requirements
        sections:
          - title: ValidateHash Requirements
            requirements:
              - id: SpdxTool-SelfTest-Hash
                title: The SelfTest shall exercise the hash command.
                tags:
                  - self-test
                justification: |
                  Self-validation of the hash command ensures it correctly generates and
                  verifies SHA-256 file hashes after installation or deployment.
                tests:
                  - SpdxTool_Hash
