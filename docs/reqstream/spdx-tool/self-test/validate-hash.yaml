# DemaConsulting.SpdxTool SelfTest ValidateHash Requirements
#
# This file contains requirements for the ValidateHash self-test step.
# It is included by the root requirements.yaml file.
---
sections:
  - title: SelfTest ValidateHash
    requirements:
      - id: SpdxTool-ST-Hash
        title: The SelfTest shall exercise the hash command.
        tags:
          - self-test
        justification: |
          Self-validation of the hash command ensures it correctly generates and
          verifies SHA-256 file hashes after installation or deployment.
        tests:
          - SpdxTool_Hash
